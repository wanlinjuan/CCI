% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc_impute.R
\name{cc_impute}
\alias{cc_impute}
\title{Consensus Clustering-Based Imputation for scRNA-seq}
\usage{
cc_impute(
  input,
  num_sampling = 50,
  prop_sampling = 0.8,
  num_clusters = 4,
  resolution = NULL,
  cutoff = 0.2,
  select_genes = NULL,
  clustering_method = "K-means",
  impute_what = NULL,
  normalize_method = "log"
)
}
\arguments{
\item{input}{Gene-by-cell expression matrix to impute.}

\item{num_sampling}{Number of repeated samplings (default = 50).}

\item{prop_sampling}{Proportion of genes sampled in each iteration (default = 0.8).}

\item{num_clusters}{Number of clusters for K-means (default = 4).}

\item{resolution}{Optional Seurat clustering resolution (not used if `num_clusters` is set).}

\item{cutoff}{Threshold to define neighboring cells (default = 0.2).}

\item{select_genes}{Vector of gene names to sample from (default = NULL means all genes).}

\item{clustering_method}{Clustering method to use: "K-means" or "SNN" (default = "K-means").}

\item{impute_what}{Logical matrix indicating which values to impute (default = zeros).}

\item{normalize_method}{Normalization method: "sct" or "log" (default = "sct").}
}
\value{
A gene-by-cell matrix with imputed values.
}
\description{
This function performs imputation on scRNA-seq data using a consensus clustering approach,
aggregating multiple clustering results over sampled gene subsets.
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE) && requireNamespace("splatter", quietly = TRUE)) {
  library(Seurat)
  library(splatter)
  set.seed(1)
  params <- newSplatParams(batchCells = 200, nGenes = 100)
  sim <- splatSimulate(params, verbose = FALSE)
  sim <- logNormCounts(sim)
  seurat_obj <- as.Seurat(sim)
  data <- GetAssayData(seurat_obj, slot = "data")
  hv_genes <- rownames(data)[1:50]
  result <- cc_impute(data, num_sampling = 5, select_genes = hv_genes, num_clusters = 2)
}
\dontrun{
imputed_data <- cc_impute(input = expression_matrix)
}
}
